//import R from 'ramda';

export default (state = {}, action) => {
    switch (action.type) {
        case '':
            
            break;
    
        default:
            break;
    }
     return { ...state, activeIndex: 0 };
};