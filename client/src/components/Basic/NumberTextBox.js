// import React, { PropTypes } from 'react'

// // const propTypes = {
// //     propTypes
// // }


// const NumberTextBox = (props) => {
//     // const textbox;
//     const handleChange = (event) => {

//         if (props.onChange)
//             props.onChange(event);
//     };
//     const keyDown =(event)=>{

//     };
//     return (
//         <input ref={(input) => textbox = input} onKeyDown={()=>keyDown} onChange={handleChange} />
//     )
// };

// NumberTextBox.propTypes = propTypes


// export default NumberTextBox;
